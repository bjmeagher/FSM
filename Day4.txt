/*
Day 4: A lesson in debugging.
Keep in mind that none of this code will run properly!
*/

// The bugs we reviewed in class
alert("Hello, world!";
vsr numbers = [1,2,3];
var num = numbers[3];
num; = num + 7;
var favNumPlus = prompt("Fav num?") + 1;
var lessThanThree = 1 > 3;

// Example syntax error
function hello(){
    alrt("Hello, world!");
};

// Example semantic error
function timesTwo(number){
    return number ** 2;
};